# Copy to .env and set a unique password before `docker compose up`.
# .env is gitignored - the Grafana admin password is never committed.
GF_ADMIN_USER=quicknotes_admin
GF_ADMIN_PASSWORD=change-me-to-something-unique
